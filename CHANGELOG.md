# Changelog

## Unreleased

- Contract: `fanxipan` public API `1.0.0` được khóa với `render`, `hydrate`, `assertContract`, `version`, `contractVersion`.
- CI: thêm E2E matrix đa nền tảng cho `examples` (Linux/Windows/macOS).
- Deprecation: `compiler-helper-dericved-typo` (`$dericved(...)`) deprecated in `0.1.0`, remove in `0.2.0`. Migration: dùng `$derived(...)`.


