# 10 - SSR & Hydration

## SSR

`fanxipan_ssr` cung cấp `renderToString` và metadata cho hydration.

## Hydration

Client runtime gắn event + khôi phục reactive bindings trên HTML có sẵn.

## Nguyên tắc

- Shared compile target cho SSR và client.
- Metadata tối thiểu để giảm payload.


